# In Progress

Beta. These skills are public on purpose: try them and say what breaks. They are excluded from the plugin and the top-level README until they graduate to a stable bucket. They get no docs pages, and they can change or disappear.

The plugin will not give you these. Install one directly:

```bash
npx skills@latest add ycanerden/habitat-bunny --skill=<name>
```

Nothing here yet. The first beta hop is empty on purpose.
